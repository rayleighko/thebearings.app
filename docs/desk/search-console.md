# 살까말까 연구소 — 검색 등록 체크리스트

운영자 확인 2026-08-19. **thebearings.app / www 를 다시 등록하지 말 것.**

www 와 desk 는 다른 호스트다. desk URL을 www 사이트맵에 넣지 않는다. `go.thebearings.app` 은 등록하지 않는다.

## 완료 (2026-08-19)

- GSC 속성 `thebearings.app` (클릭 0은 정상). 인스타 `@sal_kka_lab`, 유튜브 `@sal-kka-lab` 도 있음 — 웹 색인 대체가 아님. 그대로 둔다.
- GSC에 `https://www.thebearings.app/sitemap.xml` + `https://desk.thebearings.app/sitemap.xml` 제출됨 (desk 4페이지 발견, 처리 성공).
- 네이버 서치어드바이저 `https://www.thebearings.app` (2026-07-11) + `https://desk.thebearings.app` (2026-08-19, 소유확인·사이트맵 제출·`/` `/dev` 수집 요청).
- 네이버 desk `robots.txt` 수집됨 (`Allow: /`, sitemap 가리킴).

검색 클릭·색인 숫자는 며칠–1–2주 뒤에 붙는다. 사이트맵을 다시 제출하지 말 것.

## 하지 말 것

- `thebearings.app` / `www.thebearings.app` 재등록
- `go.thebearings.app` 등록·사이트맵 제출
- desk URL을 www 사이트맵에 섞기
- 콘솔을 매일 새로고침하며 숫자를 기다림
- 토큰을 레포 밖으로 새로 지어내기

`GOOGLE_SITE_VERIFICATION` 은 비워 둔다. 네이버 desk 토큰은 코드에 있다.

확인용: `https://desk.thebearings.app/` · `/dev` · `/blog` · `https://desk.thebearings.app/sitemap.xml`
